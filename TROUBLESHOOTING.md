# Noble60 Ledger App - 故障排除指南

## 删除应用时出现 "OSError: read error"

### 🔍 问题原因

`OSError: read error` 错误通常由以下原因引起：

1. **设备状态不正确** - 设备需要在主屏幕，而不是在应用中
2. **设备被锁定** - 需要输入 PIN 码解锁
3. **USB 连接问题** - 设备未被正确识别
4. **应用正在运行** - Noble60 应用还在运行中
5. **其他软件占用** - Ledger Live 或其他工具正在使用设备

---

## ✅ 解决方案

### 方法 1: 直接在 Ledger 设备上删除（最推荐）⭐

这是最简单、最可靠的方法，不需要任何命令行操作。

**步骤：**

1. **解锁设备**
   - 输入 PIN 码解锁 Ledger 设备

2. **进入设置**
   - 在主屏幕按右键（或左键）浏览菜单
   - 找到 **Settings** (齿轮图标 ⚙️)
   - 按两个按钮同时确认进入

3. **进入应用管理**
   - 在 Settings 中找到 **Applications** 或 **Apps**
   - 按两个按钮确认进入

4. **删除 Noble60**
   - 滚动找到 **Noble60** 应用
   - 选择它并按两个按钮
   - 选择 **Uninstall** 或 **Delete**
   - 按两个按钮确认删除

✅ **完成！应用已删除**

---

### 方法 2: 使用 Ledger Live（推荐）⭐

如果你安装了 Ledger Live 应用程序：

**步骤：**

1. **打开 Ledger Live**
   - 启动 Ledger Live 应用程序

2. **连接设备**
   - 通过 USB 连接你的 Ledger 设备
   - 输入 PIN 码解锁设备
   - 确保设备在主屏幕

3. **进入 Manager**
   - 在 Ledger Live 中点击左侧的 **Manager** 标签页
   - 等待 Ledger Live 识别你的设备

4. **删除应用**
   - 在设备应用列表中找到 **Noble60**
   - 点击应用旁边的 **Uninstall** 或垃圾桶图标
   - 在设备上确认删除操作

✅ **完成！应用已删除**

---

### 方法 3: 命令行删除（高级用户）

如果上述方法都不可行，可以尝试改进的命令行删除：

**准备工作：**

确保以下条件都满足：

- ✅ Ledger 设备已通过 USB 连接
- ✅ 设备已解锁（输入了 PIN 码）
- ✅ 设备在**主屏幕**（不在任何应用中）
- ✅ 关闭了 Ledger Live 和其他可能占用设备的软件

**如果 Noble60 应用正在运行：**

- 在设备上同时按两个按钮退出应用
- 确保回到主屏幕（显示设备名称或应用列表）

**执行删除：**

```bash
# 使用改进的删除脚本
cd /Users/jack/ledger3/noble60-ledger-app
./delete_app.sh
```

或者直接使用 Python 命令：

```bash
python3 -m ledgerblue.deleteApp --targetId 0x33100004 --appName Noble60
```

---

## 🔧 其他故障排除

### 问题：设备未被识别

**解决方案：**

1. 尝试使用不同的 USB 端口
2. 尝试使用不同的 USB 线
3. 重新启动电脑
4. 确保已安装 Ledger 驱动程序（macOS 通常不需要）

### 问题：权限错误

**macOS/Linux 解决方案：**

```bash
# 检查设备权限
ls -l /dev/hidraw* 2>/dev/null || ls -l /dev/usb/hiddev* 2>/dev/null

# 如果需要，使用 sudo（不推荐，但可以测试）
sudo python3 -m ledgerblue.deleteApp --targetId 0x33100004 --appName Noble60
```

### 问题：Python 模块未找到

**解决方案：**

```bash
# 重新安装 ledgerblue
pip3 install --upgrade ledgerblue

# 或使用 pip
pip install --upgrade ledgerblue
```

---

## 📱 不同设备的 Target ID

如果你使用的不是 Nano S Plus，请使用对应的 Target ID：

| 设备型号 | Target ID |
|---------|-----------|
| Nano S Plus | `0x33100004` |
| Nano X | `0x33000004` |
| Nano S | `0x31100004` |

修改删除命令：

```bash
# 例如，Nano X 用户：
python3 -m ledgerblue.deleteApp --targetId 0x33000004 --appName Noble60
```

---

## 🆘 仍然无法删除？

如果以上所有方法都失败了：

1. **尝试重置应用列表（不会影响资金）**
   - 在设备 Settings → Reset → Reset apps
   - 这会清除所有应用但保留你的种子词和账户

2. **联系 Ledger 支持**
   - 访问：https://support.ledger.com
   - 使用他们的官方支持渠道

3. **检查设备固件**
   - 确保固件版本与 API Level 25 (v1.5.0) 兼容
   - 可能需要更新固件

---

## ✅ 验证删除成功

删除后，确认应用已被移除：

**在设备上：**
- 浏览应用列表，Noble60 不应该出现

**使用命令行：**
```bash
# 列出设备上安装的应用
python3 -m ledgerblue.listApps --targetId 0x33100004
```

---

## 🔄 重新安装

如果需要重新安装 Noble60：

```bash
cd /Users/jack/ledger3/noble60-ledger-app
./installer_api25.sh load
```

---

**注意：** 删除和安装应用**不会影响**你的账户、私钥或资金。这些信息安全地存储在设备的种子词中。
